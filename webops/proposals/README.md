# WebOps proposals

This directory stores proposal metadata only. A proposal is not an implementation and does not authorize production changes.

A proposal with `status: pending-approval` must be represented by an open GitHub issue whose title starts with `[WEBOPS APPROVAL]`. Approval authorizes creation of a remediation branch and pull request only; it does not authorize automatic merge or direct production writes.
